# Database connection (Neon Postgres)
DATABASE_URL=postgresql://user:password@project.neon.tech/rhw-knowledge?sslmode=require

# OpenAI API key for embeddings
OPENAI_API_KEY=sk-...

# Path to crawl (defaults to P:\Data on Windows server)
CRAWL_PATH=P:\Data

# Optional: Override via .env.local for development
# NODE_ENV=development
